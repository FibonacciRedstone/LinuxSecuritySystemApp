# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'Security Access' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Security Access
  pod 'NMSSH'

  target 'Security AccessTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Security AccessUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
